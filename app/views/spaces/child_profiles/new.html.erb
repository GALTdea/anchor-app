<div class="max-w-2xl mx-auto">
  <%= render "shared/page_header",
    title: "New Child Profile",
    subtitle: "Add a child to #{@space.name}" %>

  <div class="card bg-base-100 shadow-sm">
    <div class="card-body">
      <%= render "form", space: @space, child_profile: @child_profile %>
    </div>
  </div>
</div>
