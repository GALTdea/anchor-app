<% processing_status = @results_presenter.processing_status %>
<% loop_health = @results_presenter.feedback_loop_health %>
<% insight_cards = @results_presenter.support_guide_insights %>
<% weekly_ideas = @results_presenter.weekly_ideas %>
<div class="mx-auto max-w-6xl space-y-6">
  <%= render "shared/page_header",
    title: @results_presenter.page_title,
    subtitle: @results_presenter.page_subtitle %>

  <%= render "child_profiles/shared/profile_subnav",
    space: @space,
    child_profile: @child_profile,
    current: :overview %>

  <section class="card bg-base-100 shadow-sm">
    <div class="card-body gap-5">
      <% if processing_status.in?(%w[queued processing failed]) %>
        <div class="flex flex-wrap items-center gap-2">
          <span class="<%= assessment_processing_badge_class(processing_status) %>">
            <%= assessment_processing_status_label(processing_status) %>
          </span>
        </div>
      <% end %>

      <div>
        <div class="mt-3 flex flex-col gap-3 rounded-box border border-base-300/70 bg-base-200/50 p-4 md:flex-row md:items-center md:justify-between">
          <p class="text-sm leading-6 text-base-content/75">
            <span class="font-medium text-base-content"><%= @child_profile.name %></span>
            <span class="mx-2 text-base-content/35">&bull;</span>
            <span><%= @child_profile.date_of_birth.present? ? "#{@child_profile.age} years old" : "Age not added yet" %></span>
            <span class="mx-2 text-base-content/35">&bull;</span>
            <span><%= @results_presenter.child_snapshot_source_label %> completed <%= @results_presenter.child_snapshot_age_phrase %></span>
          </p>
          <span class="badge badge-outline gap-2 border <%= loop_health.status_classes %>">
            <span aria-hidden="true"><%= loop_health.status_icon %></span>
            <%= loop_health.status_label %>
          </span>
        </div>
      </div>

      <div class="rounded-box border p-5 <%= loop_health.card_classes %>">
        <div class="flex flex-col gap-4 lg:flex-row lg:items-start lg:justify-between">
          <div class="max-w-3xl">
            <h3 class="text-base font-semibold text-base-content">
              <span aria-hidden="true"><%= loop_health.status_icon %></span>
              <%= loop_health.title %>
            </h3>
            <p class="mt-2 text-sm leading-6 text-base-content/75">
              <%= loop_health.body %>
            </p>
          </div>
          <div class="flex shrink-0 flex-wrap gap-2">
            <% if @latest_assessment_response.present? %>
              <%= link_to "Choose an Activity for Today",
                space_child_profile_recommendations_path(@space, @child_profile),
                class: "btn btn-primary btn-sm" %>
            <% elsif policy(Assessment.new(child_profile: @child_profile)).create? %>
              <%= button_to "Start onboarding assessment",
                start_onboarding_space_child_profile_assessments_path(@space, @child_profile),
                class: "btn btn-primary btn-sm" %>
            <% end %>
            <span class="btn btn-outline btn-sm btn-disabled" aria-disabled="true">
              Chat with Anchor Support AI
            </span>
          </div>
        </div>
      </div>
    </div>
  </section>

  <section class="card bg-base-100 shadow-sm">
    <div class="card-body gap-5">
      <div>
        <h2 class="text-lg font-semibold text-base-content">What Anchor Understands Right Now</h2>
        <p class="mt-1 text-sm leading-6 text-base-content/70">
          A quick overview of the main support lenses Anchor is using right now—always a working picture, not a final word.
        </p>
      </div>

      <% if (context_note = @results_presenter.support_guide_context_note).present? %>
        <p class="text-sm leading-6 text-base-content/65">
          <%= context_note %>
        </p>
      <% end %>

      <div class="grid gap-3 md:grid-cols-2">
        <% insight_cards.each do |card| %>
          <div class="rounded-box border border-base-300/70 bg-base-100 p-4" data-support-domain-card="<%= card.domain_key %>">
            <div class="flex items-start gap-4">
              <% if (image_src = safe_asset_path(card.image_asset)).present? %>
                <div class="flex h-16 w-16 shrink-0 items-center justify-center rounded-box bg-base-200/50">
                  <%= image_tag image_src, alt: "", class: "h-14 w-14 object-contain" %>
                </div>
              <% end %>
              <div class="min-w-0">
                <% if card.domain_title.present? %>
                  <p class="text-xs font-semibold uppercase tracking-wide text-base-content/50">
                    <%= card.domain_title %>
                  </p>
                <% end %>
                <p class="mt-2 text-sm leading-6 text-base-content/80"><%= card.summary %></p>
              </div>
            </div>

            <% if card.patterns.present? %>
              <ul class="mt-4 space-y-2">
                <% card.patterns.each do |pattern| %>
                  <li class="flex gap-2 text-sm leading-6 text-base-content/70">
                    <span class="mt-2 h-1.5 w-1.5 shrink-0 rounded-full bg-primary/60"></span>
                    <span><%= pattern %></span>
                  </li>
                <% end %>
              </ul>
            <% end %>

            <div class="mt-4 rounded-box bg-base-200/60 p-3">
              <p class="text-xs font-semibold uppercase tracking-wide text-base-content/50">Support direction</p>
              <p class="mt-1 text-sm leading-6 text-base-content/75"><%= card.support_direction %></p>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </section>
  <%# try this this week section %>
  <section class="card bg-base-100 shadow-sm">
    <div class="card-body gap-5">
      <div class="flex flex-wrap items-start justify-between gap-3">
        <div>
          <h2 class="text-lg font-semibold text-base-content">Try This This Week</h2>
          <p class="mt-1 text-sm leading-6 text-base-content/70">
            Small experiments—pick one, notice what shifts, keep what helps.
          </p>
        </div>
        <%= link_to "View all recommendations",
          space_child_profile_recommendations_path(@space, @child_profile),
          class: "btn btn-ghost btn-sm" %>
      </div>

      <div class="grid gap-4 lg:grid-cols-3">
        <% weekly_ideas.each do |idea| %>
          <div class="rounded-box border border-base-300/70 bg-base-100 p-4">
            <div class="flex flex-wrap items-center justify-between gap-3">
              <h3 class="text-base font-semibold text-base-content"><%= idea.title %></h3>
              <span class="badge badge-primary badge-outline"><%= idea.estimated_time %></span>
            </div>
            <p class="mt-3 text-xs font-semibold uppercase tracking-wide text-base-content/50">Why it may help</p>
            <p class="mt-1 text-sm leading-6 text-base-content/75"><%= idea.why_it_may_help %></p>
            <p class="mt-3 text-xs font-semibold uppercase tracking-wide text-base-content/50">How to try it</p>
            <p class="mt-1 text-sm leading-6 text-base-content/75"><%= idea.how_to_try_it %></p>
          </div>
        <% end %>
      </div>
    </div>
  </section>
  <div class="flex flex-wrap items-center justify-between gap-3">
    <div class="flex flex-wrap items-center gap-3">
      <%= link_to "Back to profiles", space_child_profiles_path(@space), class: "btn btn-ghost btn-sm" %>
      <%= link_to "Full profile details",
        space_child_profile_current_profile_path(@space, @child_profile),
        class: "link link-hover text-sm text-base-content/60" %>
    </div>
    <% if policy(@child_profile).destroy? && @child_profile.active? %>
      <%= button_to "Archive profile", space_child_profile_path(@space, @child_profile),
        method: :delete,
        form: { data: { turbo_confirm: "Archive #{@child_profile.name}? This can be undone later." } },
        class: "btn btn-error btn-sm" %>
    <% end %>
  </div>
</div>
